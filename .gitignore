# Generated visualizations
out/

# Julia
# Manifest.toml is intentionally tracked: this repo is an application, not a
# library, so reproducible builds (Docker / Railway) need the locked versions.
*.jl.cov
*.jl.*.cov
*.jl.mem
deps/deps.jl
.julia_history

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
